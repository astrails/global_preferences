- form_for(@global_preference) do |f|

  != f.error_messages

  != f.label :name
  != f.text_field :name

  != f.label :value
  != f.text_field :value

  != f.label :ttl
  != f.text_field :ttl

  != f.submit
